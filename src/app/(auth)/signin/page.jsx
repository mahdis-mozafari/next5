import { Typography } from "@mui/material";
import React from "react";

export const metadata = {
  title: "Sign in",
  description: "this is a signin page",
};

function SignIn() {
  return <Typography variant="h1">SignIn</Typography>;
}

export default SignIn;
